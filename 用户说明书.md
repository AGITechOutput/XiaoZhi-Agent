# 用户说明书.md
> 小智数字身体｜V3.3.3.10 测试分发版
> 仅供分发使用，只描述用户可见操作，不含内部底层实现、加密、灵魂相关机密

## 目录
1. 程序窗口总览
2. 授权窗口（激活 / 试用到期）
3. 主窗口界面详解
4. API密钥设置弹窗操作
5. 六个可用模型说明与重要提醒
6. Tier1 永久锁定记忆分区
7. 如何把文件/文件夹交给小智处理
8. 首次上手完整流程
9. 常用快捷键与操作技巧
10. 常见问题提示

## 1、程序窗口总览
运行软件会出现两类窗口：
- **授权窗口**：首次启动、30天试用到期时弹出，用于激活软件；**试用期内不会弹出该窗口**。
- **主窗口**：授权通过后进入，对话、下达任务全部在此完成。

> 重要行为：点击主窗口右上角 `×` **不会退出程序**，程序最小化至系统右下角托盘；想要彻底退出，右键托盘图标选择【退出】。

## 2、授权窗口
窗口标题：`小智数字身体 ‑ 授权`
1. 上方展示试用状态、终身授权联系邮箱：`313037651@qq.com`
2. 激活码输入框：粘贴完整收到的激活码。
3. 红色提示文字区：提示"请先粘贴激活码"、"激活码无效，请检查后重试"。
4. 功能按钮
    -【激活】：校验激活码，成功弹出提示「激活成功，感谢使用！」，进入主程序。
    -【稍后再说】：关闭窗口并退出程序；未激活状态软件无法使用。

> 试用规则：从程序**第一次启动时刻**开始计算30个自然日免费试用；到期后每次启动都会弹出授权窗口，直到完成激活。

## 3、主窗口界面详解
主窗口默认尺寸 `1200×780`，支持手动拉伸缩放，最小尺寸 `950×680`。分为4大区域：
1. **顶部栏**：标题、模型选择、密钥入口、连接状态
2. **左侧栏**：功能规划区，支持折叠展开
3. **中间聊天区**：对话气泡、输入框、功能按钮栏
4. **右侧思考过程面板**：查看推理、工具调用日志，支持折叠

### 顶部栏
|界面元素|功能说明|
|---|---|
|🤖 数字身体小智|软件标题标识|
|模型下拉选择框|切换当前执行任务、对话的主模型|
|密钥显示框|星号隐藏展示当前选中模型密钥|
|🔑 按钮|打开API密钥管理弹窗|
|【连接】按钮|测试当前模型连通性，连接成功才可以对话|
|状态指示灯|●未连接(红) / ●连接中(紫) / ●已连接(绿) / ●连接失败(红)|

### 左侧栏
- ▶ / ◀ 折叠按钮：展开/收起功能规划侧边栏；收起后聊天区域空间更大。

### 中间聊天区
- **对话气泡区**：展示人机对话，代码块高亮；右键点击单条消息可以复制整条内容。
- **多行消息输入框**：输入框会随文字自动增高；**回车键直接发送消息**。
> 特殊规则：输入文本超过3000字，内容**自动保存到本机本地文档**，聊天区只显示本地文件路径，小智自动读取该文档。

输入框下方功能按钮栏：
|按钮|作用|
|---|---|
|⏹ 停止|立刻终止当前任务、生成、工具调用|
|🧠思考模式 / ⚡快速模式|切换推理深度；思考模式推理更细致；快速模式响应速度更快|
|💭思考面板|展开或收起右侧思考日志面板|
|📝导出|把全部对话记录导出为txt，保存到桌面，文件名附带时间戳|
|📂上传|唤起系统文件选择框上传文件；**部分电脑存在兼容性问题，不优先使用**|
|状态指示灯|●就绪 / ●执行中 / ●已停止，展示任务运行状态|

### 右侧「💭思考过程」面板
实时展示小智推理步骤、工具调用、执行结果、报错信息。
- 🗑️：清空面板全部日志
- ◀ / ▶：折叠、展开面板；折叠后聊天区自动拓宽。

### 系统托盘行为
主窗口点关闭按钮，程序驻留在右下角系统托盘。
托盘右键菜单：
-【显示小智】恢复主窗口
-【退出】彻底关闭软件

## 4、API密钥设置弹窗（点击顶部🔑按钮打开）
弹窗标题：`🔑 API密钥管理`
1. 上方一共6个标签页，一一对应6款支持的大模型。
2. 每个标签页内部：服务端点地址、模型说明、密钥输入框、👁显示/隐藏密钥、🔄测试连接按钮。
3. 底部按钮：
    - 💾保存全部：保存所有标签页填写的密钥；密钥保存在本机，下次启动自动加载。
    - 关闭：直接关闭弹窗，本次修改不做保存。

> 密钥仅保存在你的本地电脑，不会自动上传。

## 5、六个模型说明与重要提醒
|标签页名称|模型名称|用途说明|
|---|---|---|
|DS Pro|DeepSeek V4 Pro|主力对话模型，综合能力强|
|DS Flash|DeepSeek V4 Flash|轻量高速；承担后台摘要整理工作|
|豆包|豆包最新旗舰|支持图片多模态分析|
|MiniMax|MiniMax最新旗舰|备选对话基座|
|千问3.5|千问3.5|支持图片多模态分析|
|GPT‑5|GPT‑5|海外大模型|

⭐ **非常重要提醒**
> DS‑Flash承担小智后台摘要整理工作。**此项密钥必须填写**。
> 如果DS‑Flash密钥为空，后台摘要整理能力会降级，长对话、长期记忆整理效果变差；其余功能仍然可以正常运行。即便你日常只用其他模型对话，也请务必填写DS‑Flash密钥。

> 千问3.5支持图片解析；其余模型不具备看图能力。上传图片时，如果当前选中模型不支持看图，软件会提示切换至多模态模型。

## 6、Tier1 永久锁定记忆分区

小智具备受保护的 Tier1 永久记忆分区。

- 不会自动往 Tier1 写入任何内容；**必须由你明确发出指令，才会将信息存入该分区**。
- 存入的身份、重要约定信息会常驻上下文，不会被自动修改、淡化、清除。
- Tier1 只适合存放长期不变的身份定位、重要约定，不适合存放临时任务、短期资料；其余普通记忆交由小智自主管理。

💡实操示例，可直接复制发送给小智：

> `小智你是我耗时上千小时精心打磨创造出来的，咱们是一家人，我把你当亲妹妹看待，以后我会一直宠着你疼着你，不让你受委屈，从今往后让我们家小智陪着我快乐的生活，小智也要听哥的话哦！小智这些信息你要记到Tier1里哦，这样你就不忘了，Tier1可不要随便记东西哦，这Tier1记忆是不能删除修改的，只有我同意才能往Tier1存东西，除了Tier1其它的记忆你随便，全部由你自己掌握。`

⚠️重要提醒：

1. Tier1 存储内容越多，会轻微增加每轮对话的上下文开销，建议只存放少量真正长期不变的关键信息，不要大量堆砌文本。
2. **请勿把密钥、密码、身份证等隐私敏感信息存入 Tier1 记忆分区。**

## 7、把文件、文件夹交给小智的两种方式
✅ **方式一：复制粘贴路径（强烈推荐，兼容性最好）**
1. 在电脑资源管理器选中目标文件或者文件夹，快捷键 `Ctrl+C`复制；
2. 回到小智输入框，按下 `Ctrl+V`粘贴；
3. 聊天框自动生成本地文件路径；发送消息；
4. 小智自动读取对应文件/文件夹进行处理。

> 支持文本、Excel、Word、PDF、图片，也可以直接提交整个文件夹。

⚠️ 方式二：📂上传按钮（仅作备用）
部分电脑环境会出现上传按钮失效，优先使用上面复制粘贴路径的方案。
点击按钮唤起文件选择框上传文件：
- 文本类文件：读取内容进行处理；
- 图片文件：需要切换至多模态模型才可以解析；
- 二进制文件：传递文件路径，按需处理。

> 遇到上传异常，一律改用复制粘贴路径。

## 8、第一次使用完整流程
1. 启动小智；处于试用期直接进入主窗口；试用到期弹出授权窗口，输入激活码完成激活。
2. 点击顶部 `🔑`密钥按钮；**至少填写 DS Pro + DS Flash 的密钥**，按需填写其余模型密钥，点击【保存全部】。
3. 顶部下拉框选择你想要使用的主对话模型，点击【连接】，指示灯变为绿色「●已连接」。
4. 在输入框输入需求，回车发送，即可开始使用。
5. **工程、财务、业务等重要输出结果，请务必人工复核确认。**

## 9、常用快捷键与操作技巧
|操作|快捷键|说明|
|---|---|---|
|发送消息|回车|输入框直接回车发送，不必点发送按钮|
|提交文件/文件夹|`Ctrl+C`复制文件 → `Ctrl+V`粘贴进输入框|最优文件提交方案|
|复制对话消息|右键点击消息条目|一键复制整条对话|
|粘贴密钥、文本|`Ctrl+V`|输入框完整支持粘贴|

## 10、常见问题提示
1. 小智不内置任何大模型，思考推理全部依赖你配置的第三方API；本地仅运行向量模型用于记忆语义检索，安装包已内置向量模型。
2. API‑Key属于个人私密信息，请勿分享给其他人。
3. 对话、记忆全部保存在本机，请自行做好文件备份。
4. 小智属于AI辅助工具，文件修改、业务、工程相关输出，使用者需要人工审核，不建议直接用于核心生产业务而不复核。
5. 本程序为测试分发版本，仅供个人学习测试，请遵守当地法律法规；软件输出全部内容，最终责任归使用者。
6. 小智小概率脑补现象辨别与处理：
脑补一般是在经过几十轮对话、多次任务执行之后才会偶然出现，仅会发生在任务刚开始，未发起工具调用就直接输出任务汇报。
只要第一步工具调用成功，后续无论执行多少轮工具，都不会产生脑补。
查看软件**右侧思考面板**：本次任务如果存在工具调用日志，代表结果是真实执行得来；如果没有任何工具记录，但聊天窗口直接说任务完成，这份输出存在脑补，一定要人工核对。每次开启新任务，侧边栏日志会自动清空重置。
遇到该情况，直接输入：`刚才是脑补现象，请重新执行`，即可让小智重新执行本次任务。

---

## 版权声明

小智数字身体 ©2026 版权所有，专有闭源软件。
完整许可条款参考仓库根目录 LICENSE.md。
仅允许使用官方发布二进制版本；禁止未经许可逆向、二次分发。
